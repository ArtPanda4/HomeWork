version: '3.7'

services:
  builder:
    build:
      context: src/
    volumes:
      - src:/opt


  prod:
    build:
      context: app/ 
    volumes:
      - /var/lib/docker/volumes/07_src/_data:/opt
    ports:
      - "8080:8080"

volumes:
  src: